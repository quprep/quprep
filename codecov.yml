coverage:
  status:
    project:
      default:
        target: auto        # must not drop more than `threshold` below previous
        threshold: 1%       # allow up to 1% overall drop without failing
    patch:
      default:
        target: 90%         # new code must be at least 90% covered
